<Window x:Class="LibraryApp.Windows.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryApp.Windows"
        mc:Ignorable="d"
        Title="Добавить книгу" Height="350" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Название книги" Margin="0,5"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,5"/>

        <Label Grid.Row="2" Content="Автор (введите ФИО)" Margin="0,5"/>
        <TextBox x:Name="AuthorBox" Grid.Row="3" Margin="0,5"/>

        <Label Grid.Row="4" Content="Жанр (введите название)" Margin="0,5"/>
        <TextBox x:Name="GenreBox" Grid.Row="5" Margin="0,5"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Добавить" Width="100" Click="AddButton_Click" Margin="5"/>
            <Button Content="Отмена" Width="100" Click="CancelButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
